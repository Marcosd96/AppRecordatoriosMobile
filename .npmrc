# Configuración de pnpm para evitar rutas largas en Windows
# Usa un store más corto en el home del usuario
store-dir=~/.pnpm-store

# Configuración adicional para reducir profundidad de rutas
node-linker=hoisted

